hostname: registry.thinkpro.vn

http:
  port: 8080

external_url: https://registry.thinkpro.vn

harbor_admin_password: Think@registry1

database:
  password: z8h1cUgtFd

data_volume: /data/registry

jobservice:
  max_job_workers: 10

notification:
  webhook_job_max_retry: 10

log:
  level: info
  local:
    rotate_count: 50
    rotate_size: 200M
    location: /data/registry-log

_version: 2.7.0

chart:
  absolute_url: disabled

upload_purging:
  enabled: true
  age: 168h
  interval: 24h
  dryrun: false
